# ── R / RStudio ──────────────────────────────────────────────
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# ── Secretos y configuracion local ───────────────────────────
.env

# ── Datos (nunca subir a GitHub) ─────────────────────────────
data/
*.xlsx
*.csv
*.json
!config/catalogos.json
!config/scraping_config.json
*.rds
*.parquet

# ── Archivos de proyecto IDE ────────────────────────────────
*.Rproj

# ── Documentos del cliente (confidenciales) ─────────────────
documents/Empleabilidad, Cristian Ortiz UNL.md

# ── Reportes auto-generados (se regeneran con scripts) ──────
documents/reporte_campos_reales.md

# ── Scripts de test auxiliares (no producción) ───────────────
script/test_*.py

# ── Archivos legacy / pruebas ────────────────────────────────
archive/

# ── Notebooks (pesados, no van al repo) ──────────────────────
*.ipynb
.ipynb_checkpoints/

# ── OS ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
